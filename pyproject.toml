[project]
name = "juducialanalytics"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "requests>=2.34.2",
    "psycopg[binary]>=3.1",
    "logging>=0.4.9.6",
]
